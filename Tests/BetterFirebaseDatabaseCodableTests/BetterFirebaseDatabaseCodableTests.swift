import XCTest
@testable import BetterFirebaseDatabaseCodable

final class BetterFirebaseDatabaseCodableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BetterFirebaseDatabaseCodable().text, "Hello, World!")
    }
}
